# ===================================================================
# CLINFEC PRESTADORES - ENVIRONMENT CONFIGURATION
# ===================================================================
# Version: 1.0.0
# Last Updated: 2024-11-19
#
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values (replace all CHANGE_ME values)
# 3. NEVER commit .env to version control (.gitignore blocks it)
# 4. Keep .env secure with chmod 600 .env
# ===================================================================

# -------------------------------------------------------------------
# APPLICATION SETTINGS
# -------------------------------------------------------------------

# Environment: production, staging, development
APP_ENV=production

# Enable debug mode (NEVER true in production!)
APP_DEBUG=false

# Application URL (no trailing slash)
APP_URL=https://prestadores.clinfec.com.br

# Application name
APP_NAME="Clinfec Prestadores"

# Timezone
APP_TIMEZONE=America/Sao_Paulo

# Locale
APP_LOCALE=pt_BR

# -------------------------------------------------------------------
# DATABASE CONFIGURATION
# -------------------------------------------------------------------

# Database connection
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=u673902663_clinfec
DB_USERNAME=u673902663_clinfec
DB_PASSWORD=CHANGE_ME_DATABASE_PASSWORD
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# Database socket (optional, deixe vazio para usar TCP)
DB_SOCKET=

# Database prefix (opcional)
DB_PREFIX=

# -------------------------------------------------------------------
# SESSION CONFIGURATION
# -------------------------------------------------------------------

# Session lifetime (minutes)
SESSION_LIFETIME=1440

# Session driver: file, database, redis
SESSION_DRIVER=file

# Session name (cookie name)
SESSION_NAME=CLINFEC_SESSION

# Session secure (set true if using HTTPS)
SESSION_SECURE=true

# Session HTTPOnly
SESSION_HTTPONLY=true

# Session SameSite: Strict, Lax, None
SESSION_SAMESITE=Lax

# Session path
SESSION_PATH=/

# Session domain (leave empty for current domain)
SESSION_DOMAIN=

# -------------------------------------------------------------------
# SECURITY SETTINGS
# -------------------------------------------------------------------

# CSRF token length
CSRF_TOKEN_LENGTH=32

# Password minimum length
PASSWORD_MIN_LENGTH=8

# Password requires uppercase
PASSWORD_REQUIRE_UPPERCASE=true

# Password requires lowercase
PASSWORD_REQUIRE_LOWERCASE=true

# Password requires numbers
PASSWORD_REQUIRE_NUMBERS=true

# Password requires special chars
PASSWORD_REQUIRE_SPECIAL=false

# Login max attempts before lockout
LOGIN_MAX_ATTEMPTS=5

# Login lockout duration (minutes)
LOGIN_LOCKOUT_DURATION=30

# -------------------------------------------------------------------
# FILE UPLOAD SETTINGS
# -------------------------------------------------------------------

# Max file upload size (MB)
UPLOAD_MAX_SIZE=10

# Allowed file extensions (comma separated)
UPLOAD_ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx

# Upload directory (relative to public/)
UPLOAD_DIR=uploads

# -------------------------------------------------------------------
# MAIL CONFIGURATION (for future use)
# -------------------------------------------------------------------

# Mail driver: smtp, sendmail, mailgun, etc
MAIL_DRIVER=smtp

# SMTP settings
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=CHANGE_ME_EMAIL@gmail.com
MAIL_PASSWORD=CHANGE_ME_EMAIL_PASSWORD
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@clinfec.com.br
MAIL_FROM_NAME="${APP_NAME}"

# -------------------------------------------------------------------
# LOGGING CONFIGURATION
# -------------------------------------------------------------------

# Log level: debug, info, notice, warning, error, critical
LOG_LEVEL=warning

# Log channel: file, syslog, errorlog
LOG_CHANNEL=file

# Log path (relative to project root)
LOG_PATH=logs

# Log filename
LOG_FILE=error.log

# Log max files to keep (rotation)
LOG_MAX_FILES=30

# -------------------------------------------------------------------
# CACHE CONFIGURATION
# -------------------------------------------------------------------

# Cache driver: file, redis, memcached
CACHE_DRIVER=file

# Cache prefix
CACHE_PREFIX=clinfec_

# OPcache enabled
OPCACHE_ENABLED=true

# Cache lifetime (seconds)
CACHE_LIFETIME=3600

# -------------------------------------------------------------------
# API CONFIGURATION (for future integrations)
# -------------------------------------------------------------------

# API enabled
API_ENABLED=false

# API rate limit (requests per minute)
API_RATE_LIMIT=60

# API token length
API_TOKEN_LENGTH=64

# -------------------------------------------------------------------
# EXTERNAL SERVICES (for future use)
# -------------------------------------------------------------------

# ViaCEP (already integrated)
VIACEP_ENABLED=true
VIACEP_URL=https://viacep.com.br/ws

# ReceitaWS (CNPJ validation - for future)
RECEITAWS_ENABLED=false
RECEITAWS_URL=https://www.receitaws.com.br/v1

# -------------------------------------------------------------------
# FTP DEPLOYMENT (for development only)
# -------------------------------------------------------------------

# FTP host
FTP_HOST=ftp.clinfec.com.br

# FTP port
FTP_PORT=21

# FTP username
FTP_USER=CHANGE_ME_FTP_USER

# FTP password
FTP_PASS=CHANGE_ME_FTP_PASSWORD

# FTP remote root (deployment target)
FTP_REMOTE_ROOT=/public_html

# FTP timeout (seconds)
FTP_TIMEOUT=30

# -------------------------------------------------------------------
# BACKUP CONFIGURATION
# -------------------------------------------------------------------

# Backup enabled
BACKUP_ENABLED=true

# Backup directory
BACKUP_DIR=backups

# Backup retention days
BACKUP_RETENTION_DAYS=30

# Backup include database
BACKUP_INCLUDE_DB=true

# Backup include files
BACKUP_INCLUDE_FILES=true

# -------------------------------------------------------------------
# MONITORING & METRICS
# -------------------------------------------------------------------

# Enable error reporting to external service
ERROR_REPORTING_ENABLED=false

# Sentry DSN (if using Sentry)
SENTRY_DSN=

# Google Analytics ID (if using GA)
GA_TRACKING_ID=

# -------------------------------------------------------------------
# MAINTENANCE MODE
# -------------------------------------------------------------------

# Maintenance mode enabled
MAINTENANCE_MODE=false

# Maintenance message
MAINTENANCE_MESSAGE="Sistema em manutenção. Voltaremos em breve."

# Allowed IPs during maintenance (comma separated)
MAINTENANCE_ALLOWED_IPS=127.0.0.1

# -------------------------------------------------------------------
# PERFORMANCE TUNING
# -------------------------------------------------------------------

# PHP memory limit
PHP_MEMORY_LIMIT=256M

# PHP max execution time (seconds)
PHP_MAX_EXECUTION_TIME=300

# PHP max input vars
PHP_MAX_INPUT_VARS=3000

# Enable query logging (dev only)
QUERY_LOG_ENABLED=false

# Slow query threshold (seconds)
SLOW_QUERY_THRESHOLD=1.0

# -------------------------------------------------------------------
# DEVELOPMENT SETTINGS (NEVER enable in production!)
# -------------------------------------------------------------------

# Enable profiler
PROFILER_ENABLED=false

# Enable debug toolbar
DEBUG_TOOLBAR_ENABLED=false

# Enable SQL query logging
SQL_LOG_ENABLED=false

# Mock external APIs
MOCK_EXTERNAL_APIS=false

# -------------------------------------------------------------------
# NOTES
# -------------------------------------------------------------------
#
# SECURITY BEST PRACTICES:
# - Change ALL default passwords
# - Use strong passwords (16+ chars, mixed case, numbers, symbols)
# - Keep this file secret (chmod 600 .env)
# - Never commit .env to Git
# - Use different credentials for dev/staging/prod
# - Rotate passwords regularly
# - Use environment variables in production when possible
#
# PRODUCTION CHECKLIST:
# - [ ] APP_ENV=production
# - [ ] APP_DEBUG=false
# - [ ] Strong DB_PASSWORD
# - [ ] SESSION_SECURE=true (requires HTTPS)
# - [ ] Change all CHANGE_ME values
# - [ ] Disable development features
# - [ ] Configure backups
# - [ ] Set up monitoring
#
# ===================================================================
